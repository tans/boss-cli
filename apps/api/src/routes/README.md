# API routes

Route modules live here and should stay thin: validate input, call package-level services, and return typed responses.

Do not place Boss browser automation or scheduler logic in route handlers.
